# 260331 Session Save Handoff

## Durable Lessons

- Fallback bugs around Telegram token resolution need tokenless plugin-config fixtures; otherwise tests prove only the happy path.
- A manual second `deliverInbound()` call does not verify restart recovery; any “recovery fixed” claim must touch `RecoveryEngine` or live restart evidence.
- Evidence docs are not valid with only screenshot + prose; raw gateway/state artifacts must exist and match the written trace IDs and refs.
- Root-cause wording must stay scoped to observed runtimes; do not escalate to “all deployments” unless repo evidence proves it.

## Saved To Gene Bank

- `/Users/owenmacmini/.codex/gene-bank/2026-03-31/222022-session-save.md`
